export interface BetAmountInputProps {
  betAmount: number;
  setBetAmount: (amount: number) => void;
}

export interface MinesInputProps {
  mines: number;
  setMines: (mines: number) => void;
}


   {
     /* ${(!!gameOver && clickBock.includes(i)) ? "cursor-not-allowed bg-violet-600" : "cursor-pointer bg-[#1a1d24]"} */
   }